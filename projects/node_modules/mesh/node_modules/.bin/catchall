#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../catchall/bin/cli" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../catchall/bin/cli" "$@"
  ret=$?
fi
exit $ret
