#!/usr/bin/env node
var argv = require("optimist").
usage("Usage: -t [type]").
demand(["t"]).
alias("t", "type").
describe("t","type of server (slave  or master)").
argv;


require(__dirname + "/../lib").start({ type: argv.type })