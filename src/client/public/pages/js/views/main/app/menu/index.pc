<div class="app-menu app-menu-{{position}}">

  <span>{{label}}</span>

  <a href="#" data-bind="{{ onClick: 'settings' | redirect() }}">
    options
  </a>
  
  <a href="#" data-bind="{{ onClick: nextPosition() }}">
    next
  </a>

</div>