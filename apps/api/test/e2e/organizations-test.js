import expect from "expect.js";

describe(__filename + "#", function() {
  it("can start a new desktop");
  it("on a free plan - cannot start a new desktop if the number of minutes has exipred");
});
